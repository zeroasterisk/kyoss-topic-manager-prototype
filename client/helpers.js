UI.body.helpers({
  currentVotes: function() {
    console.log('currentVotesX', this);

    return 0;
    /*
  },
  currentVotes: function() {
    console.log('currentVotes');
    return 0;
    /*
       var mtg = Meeting.current();
       var topicId = this._id;
       return Meeting.getTopicVotes(mtg, userId);
       */
  }
});
